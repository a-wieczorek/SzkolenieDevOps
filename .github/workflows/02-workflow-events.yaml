name: 02 - Workflow Events
on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "32 14 * * *"
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: | 
          echo ${{ github.event_name }}
          date